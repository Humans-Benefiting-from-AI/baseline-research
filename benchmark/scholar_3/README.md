# Canonical data pointer

The duplicated Scholar 3 catalog was removed from this repository's local
benchmark directory during the 2026-08-17 storage cleanup.

The canonical catalog remains in
`Schulman-Coaching/yutorah-local-pipeline`, normally checked out locally at
`~/Projects/yutorah-local-pipeline/data/twersky-moshe-catalog`.

`benchmark/prepare_batch_api_all.py` reads that canonical location directly.
Do not restore a second catalog copy here.
